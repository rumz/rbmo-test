{% extends "./admin/base.html" %}

{%block right-content%}
<div class="col-sm-12">
  <h2>Welcome, <small>{{user.get_full_name}}</small></h2>
  
  <div class="col-sm-12">
    <div class="panel panel-default">
      <div class="panel-body">
	<h3>Agencies Requirements Status - {{year}}</h3>
	<p>Below are the list of agencies and their requirements status either complete or lacking  </p>
      </div>
      <table class="table">
	<thead>
	  <tr>
	    <th>Agencies</th>
	    <th>Requirements</th>
	    <th>Remarks</th>
	  </tr>
	</thead>
	<tbody>
	  {% for agency in agency_req_stats %}
	  <tr>
	    <td>{{agency.agency_name}}</td>
	    <td>{% if agency.stat > 0 %}
	           <span class="glyphicon glyphicon-unchecked text-danger"></span>&nbsp;
		   Lacking
	        {% else %}
	           <span class="glyphicon glyphicon-check text-success"></span>&nbsp;
		   Complete
	        {% endif %}
	    </td>
	    <td>{% if agency.stat > 0 %}
	           <span class="glyphicon glyphicon-unchecked text-danger"></span>&nbsp;
		   PENDING
	        {% else %}
	           <span class="glyphicon glyphicon-check text-success"></span>&nbsp;
		   PROCESSED
	        {% endif %}
	    </td>
	  </tr>
	  {% endfor %}
	</tbody>
      </table>
    </div>

    <div class="panel panel-default">
      <div class="panel-body">
	<h3>Agencies Remaining Balances for Fiscal Year - {{year}}</h3>
	<p>Below are the list of agencies and their remaining balances for budget allocated for the year {{year}}  </p>
      </div>
      <table class="table">
	<thead>
	  <tr>
	    <th>Agencies</th>
	    <th>PS</th>
	    <th>MOOE</th>
	    <th>CO</th>
	  </tr>
	</thead>
	<tbody>
	</tbody>
      </table>
    </div>
  </div>
</div>
{%endblock%}
