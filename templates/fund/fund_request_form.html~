{%extends "./admin/base.html" %}

{% block left-content %}
  {% include "./admin/agency_tabs.html" %}
{% endblock %}

{% block right-content %}
<div class="col-sm-9">
  <h2>{{agency.name}}</h2>
  <hr/>
  <div class="col-sm-12"><!---style="background-color: rgba(204,204,204,.4)"-->
    <h3>Fund Request Form</h3>
    <form action="/agency/fund/process_fund_request" role="form" class="form-horizontal" method="POST">{% csrf_token %}
      <input type="hidden" name="agency_id" value="{{agency.id}}">
      <div class="form-group">
	<label class="col-sm-3">Year</label>
	<div class="col-sm-9">
	  <input type="hidden" name="year" value="{{year}}">
	  <span style="font-size:20px">{{year}}</span>
	</div>
      </div>

      <div class="form-group">
	<label class="col-sm-3">Select Program/Activity</label>
	<div class="col-sm-9">
	 <select id="id_activity" name="activity" class="form-control" onchange="getAllocBudget()">
	   <option value="0">-------------</option>
	   {% for activity in activities %}
	      <option value="{{activity.id}}">{{activity.activity}}</option>
	   {% endfor %}
	 </select>
	</div>
      </div>

      <div class="form-group">
	<label class="col-sm-3">Allocated Month</label>
	<div class="col-sm-9">
	  {{form.bulan}}
	</div>
      </div>

      <div class="form-group">
	<label class="col-sm-3">Allocated Budget</label>
	<div class="col-sm-9">
	  <span style="font-size: 18px;" id="budget">0.00</span>
	</div>
      </div>

      <div class="form-group">
	<label class="col-sm-3">Remaining Balance</label>
	<div class="col-sm-9">
	  <span style="font-size: 18px;" id="balance">0.00</span>
	</div>
      </div>
      
      <hr/>
      <div class="form-group">
	<div class="col-sm-12">
	  <p><b>Note:</b> Requested Fund for certain program/activity shall not exceed the remaining balance</p>
	</div>
	<label class="col-sm-3">Fund Requested</label>
	<div class="col-sm-9">
	  {{form.amount}}
	</div>
      </div>

      <div class="form-group">
	<div class="col-sm-9 col-sm-offset-3">
	  <button type="submit" class="btn btn-success">
	    Save Fund Request
	  </button>
	</div>
      </div>
      
    </form>
  </div>
</div>
{% endblock %}

{% block scripts %}
<script type="text/javascript" src="/static/js/fund_ajax.js"></script>
{% endblock %}

